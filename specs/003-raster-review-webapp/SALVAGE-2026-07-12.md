# 003 Salvage Notes — Session 2026-07-12

**Status**: parked, not merged. This branch (`claude/speckit-clarify-041a87`) ran a clarify→plan→tasks→analyze pass on feature 003 that turned out to overlap an **earlier, already-implemented** 003 pass (Session 2026-07-11), which landed via PR #5 (`004-compare-raster-runs → main`).

Most of this branch's 003 doc edits are therefore **superseded**. Do **not** merge this branch as-is — it conflicts with the implemented design (e.g. duplicate `FR-008f`, divergent `season` placement).

## What to salvage (4 net-new ideas the 07-11 / PR #5 work does NOT cover)

Re-raise these as a fresh, small 003 follow-up spec **after PR #5 lands**, renumbering FRs to avoid clashes:

1. **Canonical Club identity + fuzzy alias mapping.** A `RasterClub`/`RasterClubAlias` per scope so club names differing across wishes / capacity / assignment inputs resolve to one entity. Exact name/alias → auto-resolve; non-exact → propose closest by string similarity, user confirms, alias persisted. Needs a backfill for existing rows. (My draft: `FR-008f` — **collides** with the merged `FR-008f` "delete sources"; renumber.)

2. **Snapshot retention + latest-delete guard.** Retain snapshots indefinitely; admin manual delete; deleting the newest snapshot for a `(district, season)` requires an explicit confirmation, superseded ones delete freely. (My draft: `FR-014a`, plus `season` on `Snapshot` — note PR #5 put `season` on `InputSet`+`RasterSource`, not `Snapshot`; reconcile.)

3. **Optimizer run time limit.** 30-min default (app setting), admin-overridable per run, passed to the solver; limit-reached-without-proof → `feasible`. (My draft: `FR-010a`.)

4. **German-only UI on next-intl.** German-only first release, strings externalized, default locale `de`, domain terms stay German. (My draft: `FR-032`.) — Check whether PR #5's guided UI already implies this.

## Divergences to reconcile

- **`season`**: PR #5 → `RasterInputSet` + `RasterSource`. My draft → `RasterInputSet` + `RasterSnapshot`. A full design likely needs it on all three.
- **`FR-008f` number** used by both passes for different requirements.
- **Schema/naming**: repo uses a single `schema.postgres.prisma` with `Raster`-prefixed models (no dev `schema.prisma`). Any new models (`RasterClub`, etc.) follow that.

## Full superseded drafts

Live in this branch's `spec.md` / `plan.md` / `tasks.md` (Phase 11, T068–T086) / `data-model.md` / `contracts/api.md` / `research.md` / `quickstart.md` if the detailed wording is needed when re-raising.
